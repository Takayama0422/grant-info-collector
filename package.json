{
  "name": "grant-info-collector",
  "version": "0.1.0",
  "private": true,
  "description": "補助金・助成金の公募情報を収集し、CSV/XLSXへ整形する自動化ツール（雛形）",
  "scripts": {
    "collect": "node src/cli.js --config config/sources.json --output reports/latest",
    "test": "node --test --test-concurrency=1",
    "validate": "for f in src/*.js tests/*.js; do node --check \"$f\" || exit 1; done"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  }
}
